# Exchange Rate Prediction

## Requirements

>>Node (LTS)


## Setup

- Clone this repository on your local machine:

```bash
git clone {repo_url}
```

- Install `node` packages:

```bash
npm install
```

- Start server:

```bash
npm run dev
```
